<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Titulo (Nombre del personaje en detalles) -->
    <style name="title">
        <item name="android:textSize">24sp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/marioBlue</item>
    </style>
    <!-- Subtitulo (habilidades y más detalles y para el cardview->nombre) -->
    <style name="subTitle">
        <item name="android:textSize">18dp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/marioBlue</item>
    </style>
    <!-- Normal (contenidos habilidades, breve descripcion y mas detalles) -->
    <style name="normal">
        <item name="android:textSize">14dp</item>
        <item name="android:textColor">@color/black</item>
    </style>

    <!-- Titulo del menu de navegacion -->
    <style name="titleMenu">
        <item name="android:textSize">24dp</item>
        <item name="android:textColor">@color/black</item>
    </style>

    <!-- Subtitulo del menu de navegacion  -->
    <style name="subTitleMenu">
        <item name="android:textSize">18dp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/marioRed</item>
    </style>

</resources>